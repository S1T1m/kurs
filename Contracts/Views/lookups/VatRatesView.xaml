<UserControl x:Class="Contracts.Views.lookups.VatRatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="8">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8">
            <TextBox Width="120" Margin="0,0,8,0" Text="{Binding NewRate}" />
            <TextBox Width="200" Margin="0,0,8,0"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Поиск" />
            <Button Content="Добавить" Command="{Binding AddCmd}" Margin="0,0,8,0"/>
            <Button Content="Удалить" Command="{Binding DeleteCmd}" Margin="0,0,8,0"/>
            <Button Content="Сохранить" Command="{Binding SaveCmd}" Margin="0,0,8,0"/>
            <Button Content="Обновить" Command="{Binding RefreshCmd}"/>
        </StackPanel>
        <DataGrid ItemsSource="{Binding ItemsView}" SelectedItem="{Binding Selected}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Visibility="Collapsed"/>
                <DataGridTextColumn Header="Ставка, %" Binding="{Binding Rate}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>