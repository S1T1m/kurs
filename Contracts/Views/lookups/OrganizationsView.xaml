<UserControl x:Class="Contracts.Views.lookups.OrganizationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <DockPanel Margin="8">
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,8">
            <Button Content="Добавить" Command="{Binding AddCmd}" Margin="0,0,8,0"/>
            <Button Content="Удалить" Command="{Binding DeleteCmd}" Margin="0,0,8,0"/>
            <Button Content="Сохранить" Command="{Binding SaveCmd}" Margin="0,0,8,0"/>
            <Button Content="Обновить" Command="{Binding RefreshCmd}"/>
            <TextBox Width="200"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Поиск" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding ItemsView}" SelectedItem="{Binding Selected}" AutoGenerateColumns="False" IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding OrgId}" Visibility="Collapsed"/>
                <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Адрес" Binding="{Binding Address}" Width="2*"/>
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="*"/>
                <DataGridTextColumn Header="ИНН" Binding="{Binding Inn}" Width="*"/>
                <DataGridTextColumn Header="Р/с" Binding="{Binding BankAccount}" Width="*"/>
                <DataGridTextColumn Header="БИК" Binding="{Binding Bik}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>